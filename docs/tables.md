# Cascadify Tables

you can add an 'table' class to a `table`.
add wide to the table `<table class="table" wide>` and the width is 100%
or you add fixed-header to the table and the header will be fixed!